// Examples import the library through this shim so they compile inside this
// repo. In your app, import from "@golden-passport/ds-patternfly" instead —
// the MCP docs pipeline performs exactly that rewrite when it embeds an
// example's source.
export * from "../index.js";
