name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ── 1. TypeScript ────────────────────────────────────────────────────────────
  typecheck:
    name: TypeScript check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'pnpm'

      - run: pnpm install --frozen-lockfile
      - run: pnpm typecheck

  # ── 2. Library build ─────────────────────────────────────────────────────────
  build:
    name: Library build (tsup)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'pnpm'

      - run: pnpm install --frozen-lockfile
      - run: pnpm build

      - name: Upload dist artefact
        uses: actions/upload-artifact@v7
        with:
          name: dist-${{ github.sha }}
          path: packages/ds-patternfly/dist/
          retention-days: 7

  # ── 3. Storybook build ───────────────────────────────────────────────────────
  storybook-build:
    name: Storybook build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'pnpm'

      - run: pnpm install --frozen-lockfile
      - run: pnpm build-storybook

  # ── 4. Storybook tests (Vitest + Playwright Chromium) ────────────────────────
  storybook-tests:
    name: Storybook interaction + a11y tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'pnpm'

      - run: pnpm install --frozen-lockfile

      # Playwright browsers are downloaded to ~/.cache/ms-playwright.
      # Cache the whole directory keyed on the lockfile so we only re-download
      # when the resolved Playwright version changes.
      - name: Cache Playwright browsers
        uses: actions/cache@v5
        id: pw-cache
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}

      # needrestart on Ubuntu runners defaults to interactive mode, so the
      # apt-get that Playwright's --with-deps / install-deps invokes hangs on
      # a "restart services?" prompt that never receives input in CI. Force
      # needrestart into automatic mode so the apt step runs unattended.
      - name: Defuse needrestart (prevent apt hang)
        run: |
          sudo mkdir -p /etc/needrestart/conf.d
          printf '$nrconf{restart} = "a";\n' | sudo tee /etc/needrestart/conf.d/99-no-prompt.conf

      - name: Install Playwright Chromium + OS deps
        if: steps.pw-cache.outputs.cache-hit != 'true'
        env:
          DEBIAN_FRONTEND: noninteractive
          NEEDRESTART_MODE: a
        run: pnpm --filter @golden-passport/ds-patternfly exec playwright install chromium --with-deps

      - name: Install Chromium OS deps only (cache hit)
        if: steps.pw-cache.outputs.cache-hit == 'true'
        env:
          DEBIAN_FRONTEND: noninteractive
          NEEDRESTART_MODE: a
        run: pnpm --filter @golden-passport/ds-patternfly exec playwright install-deps chromium

      - run: pnpm test
